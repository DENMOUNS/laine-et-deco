export const logActivity = async (
  method: string,
  path: string,
  statusCode: number = 200,
  duration: number = 0,
  explicitUserId?: string
) => {
  void method;
  void path;
  void statusCode;
  void duration;
  void explicitUserId;
};
